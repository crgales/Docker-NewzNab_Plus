version: "3.1"

services:

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password

  newznab:
    image: newznab_plus
    restart: always
    environment:
      TZ: America/New_York
    ports:
      - 8088:80

